// Shared UI components (React Native compatible) — Phase 5
// Components live in apps/mobile for now; this package will grow as the
// desktop (Tauri) app is added and components need to be shared.
export {};
